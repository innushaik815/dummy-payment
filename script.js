function submitPayment() {
    // Simulate payment processing (in a real scenario, this would be replaced with actual payment processing code)
    setTimeout(function() {
        alert("Payment Completed");
    }, 2000);

    return false; // Prevent the form from actually submitting
}
